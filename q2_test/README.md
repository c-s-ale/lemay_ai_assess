### Notebook for Testing Solution

After running `pip install -r requirements.txt` (which includes `jupyter`, which may already be installed) from the `test_solution` directory, feel free to run the test soltuion!